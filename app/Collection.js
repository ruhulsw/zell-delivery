import { COLLECTIONS } from '../datas/Collections';
import OneColList from '../common/components/OneColList';

const CollectionScreen = () => {
    return <OneColList data={COLLECTIONS} />;
};

export default CollectionScreen;
